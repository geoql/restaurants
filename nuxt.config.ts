import {
  app,
  css,
  modules,
  plugins,
  runtimeConfig,
  ssr,
  typescript,
} from './config';

export default defineNuxtConfig({
  app,
  css,
  ssr,
  modules,
  plugins,
  runtimeConfig,
  typescript,
});
