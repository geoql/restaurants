import {
  app,
  components,
  css,
  devtools,
  experimental,
  nitro,
  modules,
  plugins,
  routeRules,
  runtimeConfig,
  ssr,
  typescript,
  vite,
} from './config';

export default defineNuxtConfig({
  app,
  components,
  css,
  devtools,
  experimental,
  nitro,
  modules,
  plugins,
  routeRules,
  runtimeConfig,
  ssr,
  typescript,
  vite,
});
